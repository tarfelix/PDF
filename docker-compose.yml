services:
  app:
    build: .
    expose:
      - "8000"
    environment:
      - CORS_ORIGINS=*
      - TEMP_FILE_TTL_MINUTES=30
    volumes:
      - pdf-temp:/app/tmp
    restart: unless-stopped

volumes:
  pdf-temp:

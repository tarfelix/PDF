services:
  app:
    build: .
    expose:
      - "8000"
    environment:
      - CORS_ORIGINS=*
      - TEMP_FILE_TTL_MINUTES=30
    volumes:
      - pdf-temp:/tmp/pdf-editor
    restart: unless-stopped

volumes:
  pdf-temp:
